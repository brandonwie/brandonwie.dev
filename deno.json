{
	"tasks": {
		"dev": "pnpm dev",
		"build": "pnpm build",
		"preview": "pnpm preview",
		"check": "pnpm check",
		"sync": "deno run --allow-read --allow-write scripts/sync-from-3b.ts"
	},
	"compilerOptions": {
		"lib": ["ESNext", "DOM", "DOM.Iterable"],
		"strict": true
	},
	"nodeModulesDir": "auto"
}
