# Dependencies
node_modules/

# Build outputs
.svelte-kit/
build/
dist/

# Deno
.deno/

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Cache
.cache/
.turbo/

# Lock files
yarn.lock
.playwright-mcp/

# Claude Code
.claude/

# Serena MCP local tool state (cache + memories + project.local.yml)
.serena/

# graphify output (per-repo knowledge graph — rebuild via /graphify)
graphify-out/

# context-mode plugin local sandbox + index (per-repo, rebuild on demand)
.context-mode/

# Git worktrees (canonical 3B path, ADR-035 — never committed)
.worktrees/
